services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: run-faster-db
    volumes:
      - db-data:/etc/data
    ports:
      - "5432:5432"
